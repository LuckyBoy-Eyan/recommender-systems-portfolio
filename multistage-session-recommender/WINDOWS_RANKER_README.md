# Windows MMoE / PLE 训练

解压后在项目根目录运行 `run_windows_rankers.ps1`。脚本依次训练 MMoE 和 PLE，使用
训练折0–2拟合，折3早停，最后只在冻结验证集评估。线上不可见的 `target_type` 不作为
输入；三个任务概率按 0.1/0.3/0.6 融合成一张统一 Top20。测试集不会被读取。

GTX 5060 8GB 默认 batch 4096；若显存不足改为2048。完成后传回两个输出目录。
